# API → CSV Quick Export

**90-sec demo:** https://www.loom.com/share/ac066c6d3d20445aba915ed877dbdfedt

- One-file Python tool: fetches JSON from a URL and writes a clean CSV.
- Includes a basic retry loop, timeout, and file logging (`logs/app.log`).
- Example:

- Outputs go to `data/`. Logs go to `logs/app.log`.
